#!/bin/sh
# placeholder: demo artifacts removed
# The helper script previously moved demo artifacts into this directory. The root-level archives were removed by maintainer instruction.

echo "Demo artifacts removed from repository root. See ARTIFACTS_MANIFEST.md for details."
